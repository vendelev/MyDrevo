# Спецификация бизнес-требований TZ1_04_3

## Метаинформация

- **Номер задачи:** TZ1_04_3
- **Название:** Удаление персоны с проверкой зависимостей
- **Родительская задача:** TZ1_04 — Управление персонами (CRUD)
- **Эпик:** TZ1 — Веб-сайт семейного генеалогического древа
- **Этап:** Stage 4 — Управление персонами
- **Дата создания:** 2026-01-23
- **Статус:** Черновик

## Описание проблемы/потребности

### Контекст

После реализации задач TZ1_04_1 (Создание персоны) и TZ1_04_2 (Редактирование персоны) пользователь может:

- Создавать новые персоны с многоязычными данными (русский/английский)
- Редактировать существующие персоны, исправляя ошибки и дополняя информацию
- Управлять многоязычными именами, фамилиями, отчествами с гендерными формами
- Указывать даты рождения/смерти и дополнительную информацию

Текущее состояние:

- Пользователь может создавать и редактировать персоны в своем генеалогическом древе
- Персоны могут быть связаны между собой через таблицу gen_relation (родители, дети, супруги)
- Доменная модель и репозитории поддерживают операции удаления (метод delete())
- Все операции логируются в таблицу audit_logs

### Проблема

**КРИТИЧНО:** Пользователь НЕ может удалять неактуальные или ошибочные персоны из своего дерева, потому что:

1. **Невозможность удаления ошибочных данных:** Нельзя удалить персону, созданную по ошибке или с неверными данными, которые невозможно исправить редактированием
2. **Риск нарушения целостности данных:** Удаление персоны без учета связей приведет к "висячим" связям в таблице gen_relation
3. **Отсутствие информации о последствиях:** Пользователь не видит, с какими другими персонами связана удаляемая персона
4. **Риск случайной потери данных:** При удалении персоны со множеством связей могут быть утеряны важные связи между людьми
5. **Невозможность очистки базы данных:** Нельзя удалить тестовые или неактуальные персоны, засоряющие дерево
6. **Накопление неактуальных данных:** Без возможности удаления база данных будет расти без контроля

### Примеры реальных ситуаций

**Ситуация 1: Удаление дублирующей персоны**
Пользователь случайно создал два раза одну и ту же персону: "Иван Петрович Сидоров". Одна из персон без связей — её нужно удалить. Сейчас это невозможно.

**Ситуация 2: Удаление тестовых данных**
При первом знакомстве с системой пользователь создал несколько тестовых персон ("Тест Тестович Тестов"). Теперь хочет их удалить, чтобы начать работать с реальными данными.

**Ситуация 3: Удаление персоны со связями**
Пользователь хочет удалить персону дальнего родственника, о котором нет достоверной информации. У персоны есть 2 связи (родители). Нужно удалить персону вместе со связями, но система не предупреждает о последствиях.

**Ситуация 4: Случайное удаление важной персоны**
Пользователь по ошибке пытается удалить ключевую персону дерева (прапрадед) с 20 связями. Без предупреждения о последствиях это может привести к разрушению структуры дерева.

**Ситуация 5: Неполное удаление данных**
Пользователь вручную удалил персону через базу данных, но забыл удалить связи. Теперь в таблице gen_relation остались "висячие" ссылки на несуществующую персону.

### Бизнес-ценность решения

Реализация удаления персон с проверкой зависимостей обеспечит:

- **Чистоту данных:** Возможность удалить дублирующие, тестовые или ошибочные персоны
- **Целостность базы данных:** Каскадное удаление связей предотвратит появление "висячих" ссылок
- **Информированное принятие решений:** Пользователь видит все связи персоны перед удалением
- **Защита от случайных ошибок:** Предупреждение о последствиях удаления персоны со связями
- **Гибкость управления:** Возможность удалить любую персону, даже если у неё много связей
- **Аудит удалений:** Возможность восстановления случайно удаленных данных из audit_logs
- **Понятность последствий:** Пользователь четко понимает, что будет удалено и что останется

## Функциональные требования

### FR-1. Кнопка удаления на карточке персоны

**Приоритет:** Высокий

**Описание:**
Система должна предоставлять доступ к функции удаления персоны с карточки персоны.

**Детализация:**

На карточке персоны `GET /persons/{id}` добавить:

- Кнопку "Удалить персону" в разделе навигации (рядом с "Редактировать" и "Назад к списку")
- Кнопка должна быть визуально отличимой (красный цвет, destructive стиль)
- Кнопка ведет на страницу подтверждения удаления `GET /persons/{id}/delete?lang={lang}`
- Кнопка сохраняет текущий язык интерфейса

**Требования:**

- Кнопка должна быть доступна только владельцу персоны (проверка userId)
- Визуальное оформление кнопки должно сигнализировать о серьезности действия
- Кнопка НЕ выполняет удаление напрямую — только переход на страницу подтверждения

**Критерии приемки FR-1:**

- ✅ На карточке персоны есть кнопка "Удалить персону"
- ✅ Кнопка имеет визуально отличимый стиль (красный, destructive)
- ✅ Кнопка ведет на `/persons/{id}/delete?lang={lang}`
- ✅ Текущий язык интерфейса сохраняется
- ✅ Кнопка доступна только владельцу персоны

---

### FR-2. Проверка зависимостей (связей) персоны

**Приоритет:** Высокий

**Описание:**
Система должна проверять наличие связей персоны с другими персонами перед удалением.

**Детализация:**

Создать маршрут `GET /persons/{person}/delete` с контроллером `PersonController@confirmDelete`, который:

1. Получает текущий язык из `App::getLocale()`
2. Получает ID тестового пользователя (первый пользователь из БД)
3. Загружает персону через `PersonRepository::findById($id, $language)`
4. Если персона не найдена — возвращает **404 Not Found**
5. Проверяет, что персона принадлежит текущему пользователю (безопасность)
6. Загружает все связи персоны через `RelationRepository::findByPersonId($id)`:
   - Связи, где персона = PID1 (персона как первый участник)
   - Связи, где персона = PID2 (персона как второй участник)
7. Для каждой найденной связи загружает данные связанной персоны:
   - Полное имя (на текущем языке)
   - Годы жизни (дата рождения - дата смерти)
   - Тип связи (родитель, ребенок, супруг)
8. Группирует связи по типам для удобства отображения:
   - **Родители:** связи типа PARENT (где персона = ребенок)
   - **Дети:** связи типа PARENT (где персона = родитель)
   - **Супруги:** связи типа SPOUSE
9. Передает данные в Blade-шаблон подтверждения удаления

**Требования:**

- Использовать eager loading для оптимизации загрузки связанных персон
- Группировка связей должна быть понятна нетехническому пользователю
- Если у персоны нет связей — показать упрощенное подтверждение
- Если у персоны есть связи — показать детальное предупреждение со списком

**Критерии приемки FR-2:**

- ✅ Страница подтверждения доступна по адресу `GET /persons/{id}/delete?lang={lang}`
- ✅ Если персона не существует — возвращается 404 Not Found
- ✅ Если персона принадлежит другому пользователю — возвращается 404 Not Found
- ✅ Система корректно определяет все связи персоны (как PID1, так и PID2)
- ✅ Связи группируются по типам (родители, дети, супруги)
- ✅ Для каждой связанной персоны загружаются имя и годы жизни
- ✅ Используется eager loading для оптимизации запросов

---

### FR-3. Страница подтверждения удаления с предупреждением

**Приоритет:** Высокий

**Описание:**
Система должна показывать страницу подтверждения удаления с детальной информацией о последствиях.

**Детализация:**

Создать Blade-шаблон `persons/delete.blade.php` со следующей структурой:

**Заголовок страницы:**

- Заголовок: "Удаление персоны: {Полное имя на текущем языке}"
- Подзаголовок: "(Дата рождения — дата смерти)" или "(Дата рождения — н.в.)" или "(Годы жизни неизвестны)"

**Блок предупреждения о связях (если связи есть):**

- Визуально заметный блок (оранжевый/красный цвет)
- Заголовок: "⚠️ Внимание! Эта персона связана с другими людьми"
- Текст: "У этой персоны есть {N} связей с другими персонами:"

**Список связанных персон (если связи есть):**

Группировка по типам связей:

1. **Родители (если есть):**
   - Заголовок: "👨👩 Родители:"
   - Список: "{Полное имя} ({Годы жизни})"

2. **Дети (если есть):**
   - Заголовок: "👶 Дети:"
   - Список: "{Полное имя} ({Годы жизни})"

3. **Супруги (если есть):**
   - Заголовок: "💑 Супруги:"
   - Список: "{Полное имя} ({Годы жизни})"

**Блок информации об удалении:**

- Заголовок: "Что будет удалено:"
- Список:
  - ✅ Основная информация о персоне (имя, фамилия, даты, дополнительная информация)
  - ✅ ВСЕ связи персоны с другими людьми ({N} связей)
  - ✅ Записи в таблицах: gen_person, gen_relation, gen_person_info_lang

- Заголовок: "Что НЕ будет удалено:"
- Список:
  - ❌ Имена и фамилии (могут использоваться другими персонами)
  - ❌ Связанные персоны (будут удалены только связи, не сами люди)
  - ❌ Таблицы: gen_name, gen_surname

**Блок подтверждения (если связи есть):**

- Текст: "При удалении этой персоны будут удалены ВСЕ {N} связей с указанными людьми"
- Текст: "Это действие необратимо. Вы уверены, что хотите продолжить?"

**Кнопки:**

- **Удалить персону и все связи** — красная кнопка (destructive), отправка формы на `DELETE /persons/{id}`
- **Отменить** — серая кнопка (secondary), возврат на карточку персоны `/persons/{id}?lang={lang}`

**Упрощенный вариант (если связей нет):**

- Блок информации об удалении (упрощенный)
- Текст: "У этой персоны нет связей с другими людьми"
- Вопрос: "Вы уверены, что хотите удалить эту персону?"
- Кнопки: "Удалить персону" и "Отменить"

**Требования:**

- Предупреждение должно быть визуально очень заметным
- Список связанных персон должен быть полным и понятным
- Пользователь должен четко понимать последствия удаления
- Кнопка удаления должна иметь destructive стиль
- Форма должна быть защищена от случайных кликов

**Критерии приемки FR-3:**

- ✅ Страница содержит заголовок с именем удаляемой персоны
- ✅ Если связи есть — показывается блок предупреждения
- ✅ Связи группируются по типам (родители, дети, супруги)
- ✅ Для каждой связанной персоны отображается имя и годы жизни
- ✅ Четко указано, что будет удалено, а что останется
- ✅ Кнопка удаления имеет destructive стиль
- ✅ Кнопка "Отменить" возвращает на карточку персоны
- ✅ Если связей нет — показывается упрощенный вариант
- ✅ Страница понятна нетехническому пользователю

---

### FR-4. Каскадное удаление персоны и связей

**Приоритет:** Высокий

**Описание:**
Система должна выполнять каскадное удаление персоны вместе со всеми её связями, обеспечивая целостность данных.

**Детализация:**

Создать маршрут `DELETE /persons/{person}` с контроллером `PersonController@destroy`, который:

1. Получает ID тестового пользователя (первый пользователь из БД)
2. Загружает персону через `PersonRepository::findById($id)`
3. Если персона не найдена — возвращает **404 Not Found**
4. Проверяет, что персона принадлежит текущему пользователю (безопасность)
5. Выполняет удаление в **транзакции БД** (все или ничего):

   **Шаг 1: Логирование ДО удаления (для сохранения данных):**
   - Создает запись в `audit_logs`:
     - `user_id` — ID текущего пользователя
     - `action` — DELETE_PERSON
     - `entity_type` — Person
     - `entity_id` — ID удаляемой персоны
     - `old_value` — JSON с данными персоны (полное имя, даты рождения/смерти, количество связей)
     - `new_value` — NULL
     - `created_at` — текущая дата/время

   **Шаг 2: Удаление связей персоны:**
   - Удаляет все записи из `gen_relation`, где `PID1 = {id}` OR `PID2 = {id}`
   - Использует `RelationRepository::deleteByPersonId($id)`

   **Шаг 3: Удаление дополнительной информации:**
   - Удаляет все записи из `gen_person_info_lang`, где `PID = {id}`

   **Шаг 4: Удаление персоны:**
   - Удаляет запись из `gen_person`, где `ID = {id}`
   - Использует `PersonRepository::delete($person)`

6. Если произошла ошибка на любом этапе — откатывает транзакцию БД
7. Перенаправляет на список персон `/persons?lang={lang}` с flash-сообщением успеха

**ВАЖНО: Что НЕ удаляется:**

- **НЕ удаляются** записи из `gen_name` и `gen_name_lang` (имена могут использоваться другими персонами)
- **НЕ удаляются** записи из `gen_surname` и `gen_surname_lang` (фамилии могут использоваться другими персонами)
- **НЕ удаляются** связанные персоны (удаляются только связи, не сами люди)

**Требования:**

- Все операции удаления должны выполняться в одной транзакции БД
- Порядок удаления критичен: сначала связи, потом информация, потом персона
- Логирование должно происходить ДО фактического удаления (чтобы сохранить данные)
- При ошибке транзакция должна откатываться полностью
- Нет частично удаленных данных
- Flash-сообщение должно содержать имя удаленной персоны для контекста

**Критерии приемки FR-4:**

- ✅ Форма отправляется на `DELETE /persons/{id}` и удаляет персону
- ✅ Удаление выполняется каскадно: связи → информация → персона
- ✅ НЕ удаляются записи из gen_name и gen_surname
- ✅ НЕ удаляются связанные персоны (только связи)
- ✅ Все операции выполняются в одной транзакции БД
- ✅ При ошибке транзакция откатывается полностью
- ✅ Логирование происходит ДО удаления
- ✅ После успешного удаления — редирект на `/persons?lang={lang}`
- ✅ Flash-сообщение содержит имя удаленной персоны
- ✅ Попытка удалить персону другого пользователя возвращает 404

---

### FR-5. JavaScript-подтверждение перед удалением

**Приоритет:** Средний

**Описание:**
Система должна требовать дополнительное JavaScript-подтверждение при нажатии кнопки удаления для защиты от случайных кликов.

**Детализация:**

На кнопку "Удалить персону и все связи" добавить JavaScript-подтверждение:

```javascript
// При клике на кнопку удаления
button.addEventListener('click', (event) => {
  event.preventDefault();
  const confirmed = confirm('Вы ДЕЙСТВИТЕЛЬНО уверены, что хотите удалить эту персону и все её связи? Это действие необратимо.');
  if (confirmed) {
    form.submit();
  }
});
```

**Текст подтверждения:**

- Если связи есть: "Вы ДЕЙСТВИТЕЛЬНО уверены, что хотите удалить эту персону и все её {N} связей? Это действие необратимо."
- Если связей нет: "Вы уверены, что хотите удалить эту персону? Это действие необратимо."

**Требования:**

- Подтверждение должно появляться при каждом клике на кнопку удаления
- Текст подтверждения должен быть понятным и серьезным
- При отмене подтверждения форма НЕ отправляется
- При подтверждении форма отправляется на DELETE /persons/{id}

**Критерии приемки FR-5:**

- ✅ При клике на кнопку удаления появляется JavaScript-подтверждение
- ✅ Текст подтверждения понятен и указывает на необратимость
- ✅ При отмене подтверждения удаление НЕ происходит
- ✅ При подтверждении форма отправляется на DELETE /persons/{id}
- ✅ Количество связей упоминается в тексте (если связи есть)

---

### FR-6. Flash-сообщение после успешного удаления

**Приоритет:** Средний

**Описание:**
Система должна показывать информативное flash-сообщение после успешного удаления персоны.

**Детализация:**

После успешного удаления персоны показывать flash-сообщение:

- Тип: success (зеленый цвет)
- Текст: "Персона успешно удалена: {Полное имя}"
- Если были связи: "Персона успешно удалена: {Полное имя}. Удалено связей: {N}"
- Автоматическое исчезновение через 5 секунд

**Требования:**

- Сообщение должно быть визуально заметным
- Сообщение должно содержать имя удаленной персоны для контекста
- Если были связи — указать количество удаленных связей
- Сообщение должно автоматически исчезать

**Критерии приемки FR-6:**

- ✅ После успешного удаления отображается flash-сообщение
- ✅ Сообщение содержит имя удаленной персоны
- ✅ Если были связи — указывается их количество
- ✅ Сообщение имеет тип success (зеленый цвет)
- ✅ Сообщение автоматически исчезает через 5 секунд

---

### FR-7. Аудит операции удаления

**Приоритет:** Высокий

**Описание:**
Система должна логировать операцию удаления персоны в таблицу audit_logs для отслеживания истории и возможности восстановления.

**Детализация:**

Система должна создать запись в таблице `audit_logs` со следующими данными:

- `user_id` — ID текущего пользователя (тестовый пользователь)
- `action` — DELETE_PERSON
- `entity_type` — Person
- `entity_id` — ID удаленной персоны
- `old_value` — JSON с данными ДО удаления
- `new_value` — NULL (персона удалена)
- `created_at` — текущая дата/время

**Структура JSON в old_value:**

```json
{
  "fullName": "Иван Петрович Сидоров",
  "birthDate": "1950-05-15",
  "deathDate": "2020-03-10",
  "gender": "MALE",
  "relationsCount": 5,
  "relations": [
    {"type": "PARENT", "personId": 10, "personName": "Петр Васильевич Сидоров"},
    {"type": "PARENT", "personId": 11, "personName": "Анна Ивановна Сидорова"},
    {"type": "CHILD", "personId": 20, "personName": "Мария Ивановна Петрова"},
    {"type": "CHILD", "personId": 21, "personName": "Алексей Иванович Сидоров"},
    {"type": "SPOUSE", "personId": 15, "personName": "Елена Сергеевна Сидорова"}
  ]
}
```

**Требования к логированию:**

- Логирование должно происходить ДО фактического удаления (в той же транзакции)
- В old_value сохранять достаточно информации для возможного восстановления
- Сохранять список всех удаленных связей с именами связанных персон
- Если персона не была удалена (ошибка) — запись в audit_logs также откатывается

**Критерии приемки FR-7:**

- ✅ Каждая операция удаления создает запись в audit_logs
- ✅ Запись содержит все обязательные поля
- ✅ В old_value сохраняются данные персоны и список связей
- ✅ Логирование происходит ДО удаления (в той же транзакции)
- ✅ При ошибке удаления логирование также откатывается
- ✅ JSON-структура old_value корректна и может быть распарсена

---

## Нефункциональные требования

### NFR-1. Авторизация (ограниченная на этапе 4)

**Описание:**
На этапе 4 авторизация не реализована, используется тестовый пользователь.

**Критерии:**

- Все операции удаления выполняются от имени тестового пользователя (первый в БД)
- Логика выбора пользователя централизована для легкой замены на этапе 7
- Пользователь не может удалять персон других пользователей (фильтрация по userId)

**Проверка:**

- Попытка удалить персону с несуществующим ID возвращает 404
- Попытка удалить персону другого пользователя возвращает 404

---

### NFR-2. Транзакционность удаления

**Описание:**
Операция удаления должна быть транзакционной для обеспечения целостности данных.

**Критерии:**

- Удаление персоны выполняется в одной транзакции БД
- При ошибке на любом этапе — откат всех изменений
- Нет частично удаленных данных (например, связи удалены, а персона осталась)
- Логирование в audit_logs происходит в той же транзакции

**Проверка:**

- Симулировать ошибку в середине транзакции (например, ошибка удаления персоны)
- Убедиться, что БД не изменилась (связи не удалены)
- Убедиться, что запись в audit_logs не создалась

---

### NFR-3. Понятность интерфейса для нетехнического пользователя

**Описание:**
Страница подтверждения удаления должна быть интуитивно понятна пользователю без технического бэкграунда.

**Критерии:**

- Предупреждение о последствиях визуально заметно
- Список связанных персон понятен и полон
- Четко указано, что будет удалено, а что останется
- Сообщения на русском языке, без технических терминов
- Кнопки имеют понятные названия ("Удалить", "Отменить")

**Проверка:**

- Ревью интерфейса нетехническим пользователем
- Usability-тестирование

---

### NFR-4. Аудит операции удаления

**Описание:**
Все операции удаления персон должны логироваться в таблицу audit_logs.

**Критерии:**

- Каждая операция удаления создает запись в audit_logs
- Логируются: user_id, action, entity_type, entity_id, old_value, new_value, created_at
- В old_value сохраняются данные удаленной персоны и список связей
- Логи не удаляются (остаются навсегда для истории)

**Проверка:**

- После каждой операции проверить наличие записи в audit_logs
- Проверить корректность сохраненных данных
- Проверить возможность восстановления данных из old_value

---

### NFR-5. Производительность

**Описание:**
Страница подтверждения удаления должна загружаться быстро даже для персон с множеством связей.

**Критерии:**

- Страница подтверждения загружается менее чем за 1 секунду
- Операция удаления выполняется менее чем за 2 секунды
- Используется eager loading для загрузки связанных персон
- Операция удаления оптимизирована (минимум запросов к БД)

**Проверка:**

- Измерить время загрузки страницы подтверждения для персоны с 50 связями
- Измерить время удаления персоны с 50 связями

---

### NFR-6. Безопасность удаления

**Описание:**
Система должна предотвращать несанкционированное удаление персон.

**Критерии:**

- Пользователь может удалять ТОЛЬКО свои персоны (проверка userId)
- Попытка удалить персону другого пользователя возвращает 404 (не 403, чтобы не раскрывать существование)
- Форма удаления защищена CSRF-токеном Laravel
- Нет SQL-инъекций (параметризованные запросы)

**Проверка:**

- Попытаться удалить персону с userId != текущий пользователь
- Попытаться отправить форму без CSRF-токена
- Попытаться выполнить SQL-инъекцию в параметре ID

---

## Модель предметной области

Этап 4 использует доменную модель, созданную на этапе 2. Удаление персон не требует изменений в доменной модели.

### Основные Entity

#### Person (Персона)

**Атрибуты:**

- `id: int` — уникальный идентификатор
- `userId: int` — владелец дерева
- `firstName: Name` — имя (многоязычное)
- `middleName: ?Name` — отчество (опционально)
- `surname: ?Surname` — текущая фамилия (опционально)
- `birthSurname: ?Surname` — девичья фамилия (опционально)
- `birthDate: ?DateTime` — дата рождения
- `deathDate: ?DateTime` — дата смерти
- `info: array<string, string>` — описание по языкам
- `gender: Gender` — пол персоны

**Бизнес-правила (актуальные для удаления):**

- Персона может быть удалена независимо от наличия связей
- При удалении персоны должны быть удалены все её связи (каскадное удаление)
- Имена и фамилии НЕ удаляются (могут использоваться другими персонами)

#### Relation (Связь)

**Атрибуты:**

- `personId1: int` — первая персона
- `personId2: int` — вторая персона
- `type: RelationType` — тип связи (PARENT, CHILD, SPOUSE)

**Использование при удалении:**

- Перед удалением персоны система загружает все связи, где персона участвует
- Связи группируются по типам для отображения пользователю
- При подтверждении удаления все связи удаляются каскадно

#### RelationType (Тип связи)

**Значения:**

- `PARENT` — родитель/ребенок
- `SPOUSE` — супруги

**Использование при удалении:**

- Определяет, как группировать связи на странице подтверждения
- Влияет на текст предупреждения (родители, дети, супруги)

---

## Зависимости

### Внешние зависимости

- **Laravel 12** — фреймворк для web routes, контроллеров, Blade, validation
- **PHP 8.5** — язык программирования
- **SQLite** — СУБД с данными

### Внутренние зависимости

Задача TZ1_04_3 **зависит** от:

- **TZ1_01** — требует готовую схему БД с таблицами gen_person, gen_relation, audit_logs
- **TZ1_02** — требует доменную модель и репозитории (методы delete)
- **TZ1_03** — требует интерфейс просмотра персон (карточки персон для кнопки удаления)
- **TZ1_04_1** — требует возможность создания персон (для тестирования удаления)
- **TZ1_04_2** (опционально) — редактирование персон (для исправления перед удалением)

Задача TZ1_04_3 **блокирует:**

- **Этап 5** — Управление связями (требует возможность удалить персону при удалении связей)
- **Этап 6** — Визуализация дерева (требует полноценное управление данными, включая удаление)

---

## Сценарии использования

### UC-1. Пользователь удаляет персону без связей

**Актор:** Пользователь

**Предусловия:**

- Пользователь авторизован (тестовый пользователь)
- В БД есть персона с ID=15: "Тест Тестович Тестов"
- У персоны НЕТ связей с другими персонами

**Основной сценарий:**

1. Пользователь открывает карточку персоны `/persons/15?lang=ru`
2. Видит отображение: "Тест Тестович Тестов"
3. Кликает на кнопку "Удалить персону" (красная кнопка)
4. Переходит на страницу подтверждения `/persons/15/delete?lang=ru`
5. Видит упрощенное подтверждение:
   - Заголовок: "Удаление персоны: Тест Тестович Тестов"
   - Текст: "У этой персоны нет связей с другими людьми"
   - Блок информации: "Что будет удалено: основная информация о персоне"
   - Вопрос: "Вы уверены, что хотите удалить эту персону?"
6. Кликает кнопку "Удалить персону"
7. Видит JavaScript-подтверждение: "Вы уверены, что хотите удалить эту персону? Это действие необратимо."
8. Подтверждает
9. Система выполняет удаление в транзакции:
   - Логирует операцию в `audit_logs`
   - Удаляет дополнительную информацию из `gen_person_info_lang`
   - Удаляет персону из `gen_person`
10. Система перенаправляет на список персон `/persons?lang=ru`
11. Пользователь видит flash-сообщение: "Персона успешно удалена: Тест Тестович Тестов"
12. Проверяет, что персоны нет в списке

**Постусловия:**

- Персона удалена из БД
- Связей не было, поэтому ничего не удалялось из gen_relation
- Операция залогирована в audit_logs
- Пользователь видит актуальный список персон

---

### UC-2. Пользователь удаляет персону с несколькими связями

**Актор:** Пользователь

**Предусловия:**

- В БД есть персона с ID=10: "Мария Ивановна Петрова"
- У персоны есть 5 связей:
  - 2 родителя: Иван Петрович Сидоров, Анна Сергеевна Сидорова
  - 2 детей: Алексей Иванович Петров, Елена Ивановна Смирнова
  - 1 супруг: Петр Васильевич Петров

**Основной сценарий:**

1. Пользователь открывает карточку персоны `/persons/10?lang=ru`
2. Видит отображение: "Мария Ивановна Петрова (дев. Смирнова)"
3. Кликает на кнопку "Удалить персону"
4. Переходит на страницу подтверждения `/persons/10/delete?lang=ru`
5. Видит детальное предупреждение:
   - Заголовок: "Удаление персоны: Мария Ивановна Петрова"
   - Блок предупреждения (оранжевый): "⚠️ Внимание! Эта персона связана с другими людьми"
   - Текст: "У этой персоны есть 5 связей с другими персонами:"
   - Группа "👨👩 Родители:"
     - Иван Петрович Сидоров (1925-01-15 — 2000-03-20)
     - Анна Сергеевна Сидорова (1928-05-10 — 2005-08-15)
   - Группа "👶 Дети:"
     - Алексей Иванович Петров (1975-03-10 — н.в.)
     - Елена Ивановна Смирнова (1978-08-20 — н.в.)
   - Группа "💑 Супруги:"
     - Петр Васильевич Петров (1950-02-15 — 2020-07-10)
   - Блок информации: "Что будет удалено: основная информация, ВСЕ 5 связей"
   - Предупреждение: "При удалении этой персоны будут удалены ВСЕ 5 связей с указанными людьми"
   - Вопрос: "Это действие необратимо. Вы уверены, что хотите продолжить?"
6. Читает весь список связей и последствия
7. Кликает кнопку "Удалить персону и все связи" (красная)
8. Видит JavaScript-подтверждение: "Вы ДЕЙСТВИТЕЛЬНО уверены, что хотите удалить эту персону и все её 5 связей? Это действие необратимо."
9. Подтверждает
10. Система выполняет удаление в транзакции:
    - Логирует операцию в `audit_logs` (сохраняет имя и список 5 связей в old_value)
    - Удаляет 5 связей из `gen_relation`
    - Удаляет информацию из `gen_person_info_lang`
    - Удаляет персону из `gen_person`
11. Система перенаправляет на список персон
12. Пользователь видит flash-сообщение: "Персона успешно удалена: Мария Ивановна Петрова. Удалено связей: 5"
13. Проверяет, что персоны нет в списке
14. Открывает карточки связанных персон (Иван Петрович Сидоров и др.)
15. Видит, что персоны остались, но связи с удаленной персоной исчезли

**Постусловия:**

- Персона удалена из БД
- 5 связей удалены из gen_relation
- Связанные персоны остались (удалены только связи, не сами люди)
- Операция залогирована с полным списком связей
- Целостность данных сохранена

---

### UC-3. Пользователь отменяет удаление персоны

**Актор:** Пользователь

**Предусловия:**

- В БД есть персона с ID=20: "Алексей Иванович Петров"
- У персоны есть 3 связи

**Основной сценарий:**

1. Пользователь открывает карточку персоны `/persons/20?lang=ru`
2. Кликает на кнопку "Удалить персону"
3. Переходит на страницу подтверждения `/persons/20/delete?lang=ru`
4. Видит предупреждение о 3 связях
5. Читает список связанных персон
6. Понимает, что удаление повлияет на важные связи
7. Кликает кнопку "Отменить" (серая кнопка)
8. Система перенаправляет на карточку персоны `/persons/20?lang=ru`
9. Персона НЕ удалена, все данные сохранены

**Альтернативный сценарий (отмена через JavaScript):**

5. Кликает кнопку "Удалить персону и все связи"
6. Видит JavaScript-подтверждение
7. Нажимает "Отмена" в JavaScript-подтверждении
8. Форма НЕ отправляется, остается на странице подтверждения
9. Персона НЕ удалена

**Постусловия:**

- Персона не удалена
- Связи не удалены
- Пользователь остался на карточке персоны или странице подтверждения

---

### UC-4. Система предотвращает удаление персоны другого пользователя

**Актор:** Пользователь 1

**Предусловия:**

- В БД есть два пользователя: User 1 и User 2
- У User 2 есть персона с ID=30: "Иван Иванов"
- User 1 (текущий пользователь) пытается удалить персону User 2

**Основной сценарий:**

1. User 1 пытается открыть страницу подтверждения `/persons/30/delete?lang=ru`
2. Система загружает персону с ID=30
3. Проверяет userId персоны: userId=2
4. Текущий пользователь: userId=1
5. userId не совпадают
6. Система возвращает **404 Not Found** (не 403, чтобы не раскрывать существование персоны)
7. User 1 видит страницу "Персона не найдена"

**Альтернативный сценарий (попытка прямого удаления):**

1. User 1 пытается отправить DELETE-запрос на `/persons/30`
2. Система загружает персону с ID=30
3. Проверяет userId: не совпадает
4. Возвращает 404 Not Found
5. Удаление НЕ происходит

**Постусловия:**

- Персона User 2 не удалена
- User 1 не получил доступ к персоне другого пользователя
- Безопасность сохранена

---

### UC-5. Система откатывает транзакцию при ошибке удаления

**Актор:** Пользователь

**Предусловия:**

- В БД есть персона с ID=40: "Петр Петров"
- У персоны есть 2 связи
- Симуляция ошибки: право на удаление из gen_person отозвано (для теста)

**Основной сценарий:**

1. Пользователь открывает страницу подтверждения `/persons/40/delete?lang=ru`
2. Видит предупреждение о 2 связях
3. Кликает "Удалить персону и все связи"
4. Подтверждает в JavaScript-подтверждении
5. Система начинает удаление в транзакции:
   - Логирует операцию в `audit_logs` — OK
   - Удаляет 2 связи из `gen_relation` — OK
   - Удаляет информацию из `gen_person_info_lang` — OK
   - Пытается удалить персону из `gen_person` — **ОШИБКА** (нет прав)
6. Система обнаруживает ошибку
7. Система откатывает всю транзакцию:
   - Откат записи в `audit_logs`
   - Откат удаления связей
   - Откат удаления информации
8. Система показывает сообщение об ошибке: "Не удалось удалить персону. Попробуйте еще раз или обратитесь к администратору."
9. Пользователь возвращается на страницу подтверждения
10. Проверяет карточку персоны `/persons/40?lang=ru`
11. Видит, что персона осталась, все связи на месте

**Постусловия:**

- Персона НЕ удалена
- Связи НЕ удалены (откат транзакции)
- Информация НЕ удалена (откат транзакции)
- Запись в audit_logs НЕ создана (откат транзакции)
- Целостность данных сохранена
- Нет частично удаленных данных

---

## Риски

### Риск 1. Каскадное удаление может привести к потере важных связей

**Описание:**
Пользователь может случайно удалить персону с множеством важных связей, потеряв информацию о структуре семейного дерева.

**Вероятность:** Средняя

**Влияние:** Высокое (потеря данных о связях)

**Меры снижения:**

- Показывать детальное предупреждение со списком ВСЕХ связей
- Группировать связи по типам (родители, дети, супруги) для лучшего понимания
- Требовать двойное подтверждение (страница подтверждения + JavaScript-подтверждение)
- Логировать все удаленные связи в audit_logs для возможного восстановления
- Визуально выделять предупреждение (красный/оранжевый цвет)

**План реагирования:**

- Если данные удалены по ошибке — восстановить из audit_logs (вручную)
- На этапе 7 добавить возможность отмены удаления (undo)
- На будущих этапах можно добавить "мягкое удаление" (soft delete)

---

### Риск 2. Производительность удаления персоны с большим количеством связей

**Описание:**
Персона может иметь десятки или сотни связей (например, в больших генеалогических деревьях). Удаление такой персоны может быть медленным.

**Вероятность:** Низкая (для учебного проекта)

**Влияние:** Среднее (долгое ожидание пользователя)

**Меры снижения:**

- Использовать eager loading для загрузки связанных персон
- Оптимизировать запросы удаления (batch delete)
- Измерить производительность на тестовых данных (персона с 100 связями)
- Добавить индексы на поля PID1 и PID2 в таблице gen_relation

**План реагирования:**

- Если удаление медленное — оптимизировать запросы
- Добавить progress bar для длительных операций удаления
- Рассмотреть асинхронное удаление (background job) для персон с более чем 50 связями

---

### Риск 3. Ошибки транзакции могут привести к частично удаленным данным

**Описание:**
Если транзакция не откатывается корректно при ошибке, могут остаться частично удаленные данные (связи удалены, а персона осталась).

**Вероятность:** Низкая (если транзакции реализованы правильно)

**Влияние:** Высокое (нарушение целостности данных)

**Меры снижения:**

- Использовать Laravel DB::transaction() для гарантированного отката
- Тестировать откат транзакции при различных ошибках
- Добавить проверки целостности данных (foreign key constraints)
- Логировать все ошибки транзакций

**План реагирования:**

- Если обнаружены частично удаленные данные — создать скрипт для восстановления целостности
- Добавить проверку целостности данных в тестах
- Добавить мониторинг ошибок транзакций

---

### Риск 4. Пользователь может не понять последствия удаления

**Описание:**
Пользователь может не прочитать предупреждение о связях и случайно удалить важную персону.

**Вероятность:** Средняя

**Влияние:** Среднее (потеря данных)

**Меры снижения:**

- Сделать предупреждение максимально заметным (яркий цвет, большой шрифт)
- Использовать понятные формулировки без технических терминов
- Требовать двойное подтверждение
- Показывать количество связей в тексте подтверждения
- Добавить иконки (⚠️ для предупреждения)

**План реагирования:**

- Если пользователи жалуются на случайные удаления — усилить предупреждение
- Провести usability-тестирование с нетехническими пользователями
- Рассмотреть добавление задержки перед активацией кнопки удаления (например, 3 секунды)

---

### Риск 5. Невозможность восстановления удаленных данных без audit_logs

**Описание:**
Если audit_logs не работает или данные логируются некорректно, восстановить удаленные данные будет невозможно.

**Вероятность:** Низкая

**Влияние:** Высокое (потеря данных навсегда)

**Меры снижения:**

- Тестировать логирование в audit_logs в каждом тесте
- Проверять корректность JSON-структуры в old_value
- Сохранять достаточно информации для восстановления (имя, даты, связи)
- Добавить резервное копирование БД (backup)

**План реагирования:**

- Если audit_logs не работает — исправить немедленно
- Добавить проверку наличия записи в audit_logs после каждого удаления
- Рассмотреть добавление мягкого удаления (soft delete) как альтернативы

---

## Критерии приемки

### Критерии приемки задачи

Задача считается завершенной, если выполнены **все** следующие критерии:

#### Критерии по функциональности удаления

- ✅ На карточке персоны есть кнопка "Удалить персону"
- ✅ Кнопка имеет визуально отличимый стиль (красный, destructive)
- ✅ Кнопка ведет на страницу подтверждения `/persons/{id}/delete?lang={lang}`
- ✅ Если персона не существует — возвращается 404 Not Found
- ✅ Если персона принадлежит другому пользователю — возвращается 404 Not Found

#### Критерии по странице подтверждения

- ✅ Страница подтверждения доступна по адресу `/persons/{id}/delete?lang={lang}`
- ✅ Страница показывает заголовок с именем удаляемой персоны
- ✅ Если связи есть — показывается блок предупреждения (оранжевый/красный)
- ✅ Связи группируются по типам (родители, дети, супруги)
- ✅ Для каждой связанной персоны отображается имя и годы жизни
- ✅ Четко указано, что будет удалено, а что останется
- ✅ Если связей нет — показывается упрощенный вариант
- ✅ Кнопка "Удалить" имеет destructive стиль
- ✅ Кнопка "Отменить" возвращает на карточку персоны

#### Критерии по каскадному удалению

- ✅ Форма отправляется на `DELETE /persons/{id}` и удаляет персону
- ✅ Удаление выполняется каскадно: связи → информация → персона
- ✅ Удаляются все связи, где персона = PID1 OR PID2
- ✅ НЕ удаляются записи из gen_name и gen_surname
- ✅ НЕ удаляются связанные персоны (только связи)
- ✅ Все операции выполняются в одной транзакции БД
- ✅ При ошибке транзакция откатывается полностью

#### Критерии по JavaScript-подтверждению

- ✅ При клике на кнопку удаления появляется JavaScript-подтверждение
- ✅ Текст подтверждения понятен и указывает на необратимость
- ✅ Количество связей упоминается в тексте (если связи есть)
- ✅ При отмене подтверждения удаление НЕ происходит
- ✅ При подтверждении форма отправляется на DELETE /persons/{id}

#### Критерии по flash-сообщениям

- ✅ После успешного удаления отображается flash-сообщение
- ✅ Сообщение содержит имя удаленной персоны
- ✅ Если были связи — указывается их количество
- ✅ Сообщение имеет тип success (зеленый цвет)
- ✅ Сообщение автоматически исчезает через 5 секунд
- ✅ После удаления происходит редирект на `/persons?lang={lang}`

#### Критерии по аудиту

- ✅ Каждая операция удаления создает запись в audit_logs
- ✅ Запись содержит все обязательные поля (user_id, action, entity_type, entity_id, old_value, new_value, created_at)
- ✅ В old_value сохраняются данные персоны и список связей
- ✅ JSON-структура old_value корректна и может быть распарсена
- ✅ Логирование происходит ДО удаления (в той же транзакции)
- ✅ При ошибке удаления логирование также откатывается

#### Критерии по безопасности

- ✅ Пользователь не может удалять персон других пользователей (404)
- ✅ Форма удаления защищена CSRF-токеном Laravel
- ✅ Все операции выполняются в транзакциях БД
- ✅ Нет SQL-инъекций (параметризованные запросы)
- ✅ Нет XSS-уязвимостей (экранирование в Blade)

#### Критерии по тестам

- ✅ Написаны E2E тесты для удаления персоны без связей
- ✅ Написаны E2E тесты для удаления персоны с несколькими связями
- ✅ Тесты проверяют каскадное удаление (связи удаляются)
- ✅ Тесты проверяют, что связанные персоны НЕ удаляются
- ✅ Тесты проверяют, что имена/фамилии НЕ удаляются
- ✅ Тесты проверяют откат транзакции при ошибке
- ✅ Тесты проверяют обработку ошибки 404 (несуществующая персона)
- ✅ Тесты проверяют обработку ошибки 404 (персона другого пользователя)
- ✅ Тесты проверяют логирование в audit_logs

#### Критерии по коду

- ✅ Код соответствует стилю проекта (.ai/Rule/CodeStyle.md)
- ✅ Код проходит статический анализ PHPStan без ошибок
- ✅ Код проходит проверку PHP_CodeSniffer без ошибок
- ✅ Контроллеры используют репозитории (не работают с БД напрямую)
- ✅ Все операции записи выполняются в транзакциях

#### Критерии по производительности

- ✅ Страница подтверждения загружается менее чем за 1 секунду
- ✅ Операция удаления выполняется менее чем за 2 секунды
- ✅ Используется eager loading для загрузки связанных персон

---

## Не входит в реализацию

Следующие функции **НЕ входят** в реализацию текущей задачи:

### Не входит в TZ1_04_3

- ❌ **Создание персоны** — уже реализовано в задаче TZ1_04_1
- ❌ **Редактирование персоны** — реализовано в задаче TZ1_04_2
- ❌ **Управление связями персоны** — будет реализовано на Stage 5
- ❌ **Визуализация дерева** — будет реализована на Stage 6
- ❌ **Авторизация и аутентификация** — будет реализована на Stage 7
- ❌ **Мягкое удаление (soft delete)** — может быть добавлено на Stage 7
- ❌ **История изменений персоны (детальный просмотр)** — не входит в эпик TZ1
- ❌ **Отмена удаления (undo)** — может быть добавлено на Stage 7
- ❌ **Массовое удаление персон** — не входит в эпик TZ1
- ❌ **Восстановление персоны из audit_logs через UI** — не входит в эпик TZ1
- ❌ **Экспорт удаленных персон** — не входит в эпик TZ1
- ❌ **Уведомления о удалении** — не входит в эпик TZ1

### Допущения текущей задачи

На текущем этапе допускаются следующие упрощения:

- ✅ **Один тестовый пользователь** — авторизация будет на Stage 7
- ✅ **Жесткое удаление (hard delete)** — мягкое удаление может быть добавлено на Stage 7
- ✅ **Восстановление только через БД** — UI для восстановления может быть добавлен на Stage 7
- ✅ **Простое подтверждение** — более сложные механизмы защиты (задержка кнопки) могут быть добавлены позже
- ✅ **Синхронное удаление** — асинхронное удаление (background job) может быть добавлено при необходимости

---

## Дополнительные замечания

### Важные особенности реализации

1. **Каскадное удаление критично:** Без каскадного удаления связей возникнут "висячие" ссылки, нарушающие целостность данных.

2. **Транзакционность обязательна:** Без транзакций возможны частично удаленные данные (связи удалены, а персона осталась).

3. **Детальное предупреждение обязательно:** Пользователь должен ЧЕТКО понимать последствия удаления персоны со связями.

4. **Аудит для восстановления:** Логирование в audit_logs с полным списком связей позволит восстановить случайно удаленные данные.

5. **Двойное подтверждение важно:** Страница подтверждения + JavaScript-подтверждение защищают от случайных удалений.

6. **Группировка связей улучшает понимание:** Пользователь лучше понимает последствия, когда связи сгруппированы (родители, дети, супруги).

### Рекомендации по реализации

1. **Начать с проверки зависимостей:** Сначала реализовать загрузку и группировку связей, затем удаление.

2. **Использовать eager loading:** Загрузка всех связанных персон одним запросом критична для производительности.

3. **Тестировать транзакции тщательно:** Симулировать ошибки на разных этапах и проверять откат.

4. **Логировать детально:** В audit_logs сохранять достаточно информации для восстановления (включая список связей).

5. **Визуальное оформление важно:** Предупреждение должно быть очень заметным, кнопка удаления — destructive.

6. **Использовать существующие компоненты:** Blade-компоненты для предупреждений и кнопок можно переиспользовать.

---

## Глоссарий

- **Удаление персоны** — удаление записи персоны из базы данных
- **Каскадное удаление** — удаление связанных записей вместе с основной (связи удаляются вместе с персоной)
- **Связь (Relation)** — запись в таблице gen_relation, связывающая две персоны
- **Зависимости** — связи персоны с другими персонами
- **Предупреждение** — информация о последствиях удаления, показываемая пользователю
- **Страница подтверждения** — промежуточная страница, где пользователь подтверждает удаление
- **JavaScript-подтверждение** — дополнительное подтверждение через alert() для защиты от случайных кликов
- **Flash-сообщение** — временное уведомление пользователю о результате операции
- **Аудит (audit log)** — журнал всех действий пользователя для отслеживания истории
- **Транзакция БД** — группа операций, которые выполняются вместе (все или ничего)
- **Откат транзакции** — отмена всех изменений при ошибке
- **404 Not Found** — HTTP-статус, означающий, что запрашиваемый ресурс не найден
- **Destructive стиль** — визуальное оформление кнопки (красный цвет), сигнализирующее о серьезности действия
- **Eager loading** — загрузка связанных данных одним запросом для оптимизации производительности
- **Жесткое удаление (hard delete)** — полное удаление записи из БД
- **Мягкое удаление (soft delete)** — пометка записи как удаленной без фактического удаления
- **Висячие ссылки** — ссылки на несуществующие записи, нарушающие целостность данных

---

**Конец спецификации TZ1_04_3**
